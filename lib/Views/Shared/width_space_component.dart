import 'package:flutter/material.dart';

class WidthSpaceComponent extends StatelessWidget {
  const WidthSpaceComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 10,
    );
  }
}
